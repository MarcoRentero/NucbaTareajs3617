// 4- Crear una función que reciba un número por parámetro e imprima por consola todos los números desde el 0 hasta llegar
// a ese número.
function ImprimeelNumero(num) {
    for (i = 0; i<= num; i++){
        console.log(i);
    }
}
ImprimeelNumero(15);